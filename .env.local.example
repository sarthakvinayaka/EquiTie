# OpenAI API key — get one at https://platform.openai.com/api-keys
# Without this key the app runs in Demo Mode: answers are structured templates,
# not LLM-phrased. All numbers remain deterministically correct.
OPENAI_API_KEY=sk-...
